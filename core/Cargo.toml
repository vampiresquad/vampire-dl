[package]
name = "vampire-core"
version = "0.1.0"
edition = "2021"
description = "Core engine for VampireDL"

[dependencies]
reqwest = { version = "0.11", features = ["stream"] }
tokio = { version = "1", features = ["full"] }
